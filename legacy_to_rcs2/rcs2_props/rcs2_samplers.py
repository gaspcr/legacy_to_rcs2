# Placeholder for task #5. BandSampler analogous to dp0_samplers.BandSampler:
# sample() returns {median, rms, seeing, exp_time, zero_point, gain}.
# For now median is fixed to 0.0 (option A); switch to per-frame measured
# value (option B) once production sky-subtract logic is identified.
